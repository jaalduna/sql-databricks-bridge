# =============================================================================
# Brasil Configuration
# Country: Brasil
# Database: BR_KWP
# Server: KTCLSQL003.KT.group.local
# =============================================================================

# Database connection (specific to Brasil)
server: "KTCLSQL003.KT.group.local"
database: "BR_KWP"

# Query parameters for Brasil
parameters:
  schema: "dbo"
  start_period: 202501
  end_period: 202512
  lookback_months: 24
  country: "brasil"
  country_code: "BR"

# Databricks destination (Unity Catalog)
destination:
  catalog: "000-sql-databricks-bridge"
  schema: "brasil"
  volume: "files"
