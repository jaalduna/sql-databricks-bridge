# =============================================================================
# KTCLSQL002 Server Configuration
# Server: KTCLSQL002.KT.group.local (Bolivia server)
# Database: PS_LATAM (server-wide shared database)
# =============================================================================

# Database connection (server-level PS_LATAM)
server: "KTCLSQL002.KT.group.local"
database: "PS_LATAM"

# Query parameters
parameters:
  schema: "dbo"
  start_period: 202501
  end_period: 202512
  lookback_months: 24
  server_id: "KTCLSQL002"

# Databricks destination (Unity Catalog)
destination:
  catalog: "000-sql-databricks-bridge"
  schema: "KTCLSQL002"
  volume: "files"
