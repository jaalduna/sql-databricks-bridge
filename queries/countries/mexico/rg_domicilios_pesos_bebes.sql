-- mexico: rg_domicilios_pesos_bebes
-- note: using select * - column list to be added after schema inspection

select
    *
from rg_domicilios_pesos_bebes
-- where <time_column> >= {start_param} and <time_column> <= {end_param}
