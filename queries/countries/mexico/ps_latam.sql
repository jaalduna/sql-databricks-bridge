-- mexico: ps_latam
-- STATUS: TABLE DOES NOT EXIST
-- Alternative: Use PS_LATAM database tables (psData, psDataMexico, etc.)

-- Placeholder to prevent extraction error
SELECT 'TABLE_DOES_NOT_EXIST' as error_message WHERE 1=0
