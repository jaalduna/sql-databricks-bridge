-- bolivia: nac_ato
-- columns: 14
-- database: BO_NAC (already have access)

select
    coef01,
    coef02,
    coef03,
    datacoleta,
    duplicado,
    frmcompra,
    idato,
    numpack,
    preco,
    qtde,
    unipack,
    ventasuelta,
    volume,
    vsfrmcompra
from BO_NAC.dbo.nac_ato
